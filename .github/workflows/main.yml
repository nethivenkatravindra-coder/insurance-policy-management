name: Insurance CI

on:

  push:

    branches:
      - main

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v4

    - name: Build Docker Image

      run: docker build -t insurance-app ./App

    - name: Test Container

      run: docker run -d -p 8080:80 insurance-App
